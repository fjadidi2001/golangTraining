package main
func main(){
    runtime.GOMAXPROCS(1) //The maximum amount we give to the program for a proccess
    for {
        go fmt.Print(0) //task 1
        fmt.Print(1) //task 2
        }
}
// if we change GOMAX to 2 we can see program run paraller
